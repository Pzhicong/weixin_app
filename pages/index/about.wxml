<!--logs.wxml-->
<view class="container log-list">
  <block wx:for="{{logs}}" wx:key="*this">
    <text class="log-item">{{index + 1}}. {{item}}</text>
  </block>
  <view>{{object.text}}1237</view>
</view>
